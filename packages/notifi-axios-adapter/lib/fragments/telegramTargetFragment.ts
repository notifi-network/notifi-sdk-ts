export const telegramTargetFragment = `
fragment telegramTargetFragment on TelegramTarget {
  id
  isConfirmed
  name
  telegramId
  confirmationUrl
}
`.trim();

export const telegramTargetFragmentDependencies = [];
