export default function Home() {
  return (
    <main
      // TODO: depends on NotifiContext
      dark-mode="false"
      className="notifi-dapp flex min-h-screen flex-col items-center justify-between p-24 bg-notifi-page-bg"
    >
      <div className="text-gray-950">We’re excited, start from here</div>
    </main>
  );
}
